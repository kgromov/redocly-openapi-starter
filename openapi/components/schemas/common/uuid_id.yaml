type:
  - string
  - 'null'   # relevant only for create operation; in perfect world it should be a separate schema - e.g. ProductRequest
format: uuid
description: System generated unique identifier for the resource.
readOnly: true